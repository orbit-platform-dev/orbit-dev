# --- Core API ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20          # zero-config dev database
asyncpg>=0.30            # PostgreSQL driver (docker-compose / production)
pydantic>=2.9
pydantic-settings>=2.6
redis>=5.2
httpx>=0.27
python-jose[cryptography]>=3.3   # Clerk JWT verification

# --- AI orchestration (optional at runtime; the API degrades gracefully) ---
# Provider-agnostic via PydanticAI. The base install supports Anthropic, Google
# (Gemini), OpenAI and Ollama out of the box; Groq/Mistral/etc. need their extra,
# e.g.  pip install "pydantic-ai-slim[groq]".
pydantic-ai>=2.0,<3
langgraph>=1.0,<2
anthropic>=0.40
